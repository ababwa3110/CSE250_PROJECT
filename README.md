# Prerequisites

Before running this project, ensure you have the following installed:

Node.js (v14 or higher)

MariaDB Server (via XAMPP or standalone installer)

HeidiSQL (or any similar SQL client)
